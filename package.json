{
  "name": "weather-ranking-app",
  "private": true,
  "packageManager": "bun@1.3.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run generate && turbo build",
    "dev": "bun run generate && turbo dev",
    "generate": "bun run generate:schema && bun run generate:gql",
    "generate:gql": "bun run --filter frontend generate",
    "generate:schema": "bun run --filter backend generate:schema",
    "test": "turbo test",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.3"
  }
}
